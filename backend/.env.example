# Database
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# JWT Secret (für Produktion ändern!)
JWT_SECRET=lifeoptima-dev-secret-change-in-production

# Google Fit OAuth (optional)
# Erstelle eine OAuth 2.0 Client ID in Google Cloud Console:
# https://console.cloud.google.com/apis/credentials
# Authorized redirect URIs: http://localhost:4000/api/google-fit/callback
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:4000/api/google-fit/callback
